/*
function voca() {
  let signIn = "${signIn}";
  if (signIn == "") {
    alert("로그인 후 이용할 수 있습니다.");
    location.href="${cpath}/login.do"  // 경로 수정해야 함
  }
  else {
    location.href="${cpath}/service.do"  // 경로 수정해야 함
  }
}

function test() {
  let signIn = "${signIn}";
  if (signIn == "") {
    alert("로그인 후 이용할 수 있습니다.");
    location.href="${cpath}/login.do"  // 경로 수정해야 함
  }
  else {
    location.href="${cpath}/service.do"  // 경로 수정해야 함
  }
}

function group() {
  let signIn = "${signIn}";
  if (signIn == "") {
    alert("로그인 후 이용할 수 있습니다.");
    location.href="${cpath}/login.do"  // 경로 수정해야 함
  }
  else {
    location.href="${cpath}/service.do"  // 경로 수정해야 함
  }
}
*/





